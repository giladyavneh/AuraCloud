{
  "name": "logic",
  "version": "1.0.0",
  "description": "AuraCloud central logic (Brain) — cross-references project requirements, user permissions, and cached cloud data.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "utils": "*",
    "dotenv": "^17.4.2"
  }
}
